// const jwt = require('jsonwebtoken');

// require('dotenv').config();

// function jwtCustomer (customer_id) {
//     const payload = {
//         user: customer_id,
//     };

//     return jwt.sign(payload, `${process.env.jwtSecret}`, { expiresIn: '1h' });
// }

// module.exports = jwtCustomer;
